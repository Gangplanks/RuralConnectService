<%= form_for(@user_scheme_favorite) do |f| %>
  <% if @user_scheme_favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_scheme_favorite.errors.count, "error") %> prohibited this user_scheme_favorite from being saved:</h2>

      <ul>
      <% @user_scheme_favorite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :scheme_id %><br>
    <%= f.number_field :scheme_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
